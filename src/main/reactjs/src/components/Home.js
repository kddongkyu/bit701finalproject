import React from 'react';
import {Alert} from "react-bootstrap";

function Home(props) {
    return (
        <div>
            <Alert variant={"danger"}>Home</Alert>
        </div>
    );
}

export default Home;