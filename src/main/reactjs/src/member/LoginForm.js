import React from 'react';
import {Alert} from "react-bootstrap";


function LoginForm(props) {
    return (
        <div>
            <Alert variant={"primary"}>LoginForm</Alert>
        </div>
    );
}

export default LoginForm;