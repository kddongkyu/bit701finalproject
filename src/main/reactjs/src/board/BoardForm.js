import React from 'react';
import Alert from 'react-bootstrap/Alert';

function BoardForm(props) {
    return (
        <div>
            <Alert variant="success">BoardForm</Alert>
        </div>
    );
}

export default BoardForm;