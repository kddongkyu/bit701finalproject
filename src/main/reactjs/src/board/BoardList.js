import React from 'react';
import {Alert} from "react-bootstrap";

function BoardList(props) {
    return (
        <div>
            <Alert variant={"success"}>BoardList</Alert>
        </div>
    );
}

export default BoardList;